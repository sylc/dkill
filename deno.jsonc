{
  "tasks": {
    "test": "deno test --allow-run --allow-net",
    "dev": "deno run --unstable --allow-run --allow-net ./cli.ts",
    "release": "deno run -A https://deno.land/x/release_up@0.3.2/cli.ts --regex \"(?<=@)(.*)(?=\/cli)\" --github --versionFile --changelog"
  }
}
